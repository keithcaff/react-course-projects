console.log('app.js is running');

var template = <h1> Chaned app.js again77!</h1>;
var appRoute = document.getElementById('app');

//JSX to render, div to render it in
ReactDOM.render(template, appRoute);

